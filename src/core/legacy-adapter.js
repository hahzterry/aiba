(function(global){
  "use strict";

  const runtime=global.AIBA&&global.AIBA.runtime;
  if(!runtime||!runtime.experimental)throw new Error("AIBA legacy adapter requires the experimental runtime");

  runtime.attachLegacy({
    $,
    G,
    DIFFS,
    TALK_PRE,
    RACK_RUSH_LEVELS,
    RACK_RUSH_RUNS_KEY,
    RACK_RUSH_SPEED_RUNS_KEY,
    RACK_RUSH_SPEED_TARGET,
    VISION,
    RACKS,
    DEEPS,
    HOOP,
    COURT_ATTACK_DIR,
    GAME_VERSION,
    GAME_NAME,
    GAME_SEED,
    scene,
    balls,
    rivals,
    deepBalls,
    player,
    passer,
    handBall,
    pBall,
    hands,
    confPts,
    CAM,
    P,
    rig,
    camTarget,
    TILT,
    V3,
    seededRandom,
    buildSeq,
    benchSetup,
    startAIShow,
    stars,
    aiProb,
    applyStarStyle,
    shotProfileText,
    playerShotProfile,
    resetFinalRun,
    resetAudioCueMemory,
    resetRackBalls,
    stopCelebrate,
    ensureAudio,
    hidePanel,
    music,
    resetProgressiveSceneForRun,
    enterArenaAudio,
    leaveArenaAudio,
    faceTo,
    applyCamMode,
    shotEye,
    autoFrameCam,
    glideTo,
    startPreGameShow,
    broadcastSting,
    paSay,
    playPregameCountdownCue,
    sGo,
    calibrateTilt,
    readyBall,
    toast,
    sBeep,
    updDotsUI,
    updPowerUI,
    updTargetUI,
    playAudioEvent,
    crowdSwell,
    doRelease,
    sBuzz,
    startConfetti,
    cheerSound,
    airhorn,
    djSay,
    endHero,
    organCharge,
    scoreQuoteMarkup,
    showPanel,
    startVictoryCine,
    showMenu,
    goDiff,
    getPassing:()=>passing,
    setPassing:value=>{passing=value;},
    setCamSnap:value=>{camSnap=!!value;},
    getLowBeep:()=>lowBeep,
    setLowBeep:value=>{lowBeep=value;},
    getScenePreset:()=>currentScenePreset,
    getWeather:()=>currentWeather
  });
})(window);
